<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hbust.mapper.ClazzsMapper">

    <!-- 条件分页查询 -->
    <select id="findAll" resultType="com.hbust.pojo.Clazz">
        SELECT * FROM clazz
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="begin != null and begin != ''">
                AND begin_date &gt;= #{begin}
            </if>
            <if test="end != null and end != ''">
                AND end_date &lt;= #{end}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 统计总数 -->
    <select id="countAll" resultType="java.lang.Long">
        SELECT COUNT(*) FROM clazz
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="begin != null and begin != ''">
                AND begin_date &gt;= #{begin}
            </if>
            <if test="end != null and end != ''">
                AND end_date &lt;= #{end}
            </if>
        </where>
    </select>


    <!-- 根据ID查询班级 -->
    <select id="selectById" resultType="com.hbust.pojo.Clazz">
        SELECT id, name, room, begin_date, end_date, master_id, subject
        FROM clazz
        WHERE id = #{id}
    </select>

    <!-- 根据名称查询班级（新增） -->
    <select id="selectByName" resultType="com.hbust.pojo.Clazz">
        SELECT id, name, room, begin_date, end_date, master_id, subject,
               create_time, update_time
        FROM clazz
        WHERE name = #{name}
    </select>

    <!-- 插入班级 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO clazz (name, room, begin_date, end_date, master_id, subject,
                           create_time, update_time)
        VALUES (#{name}, #{room}, #{beginDate}, #{endDate}, #{masterId}, #{subject},
                #{createTime}, #{updateTime})
    </insert>

    <!-- 根据ID删除班级 -->
    <delete id="deleteById">
        DELETE FROM clazz
        WHERE id = #{id}
    </delete>

</mapper>